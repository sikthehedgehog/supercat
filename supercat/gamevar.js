// Container for global objects
// My workaround for lack of namespaces, shut up :P
var game = new function() { return this; };

// Level data is stored in this array
game.level_data = new Array;
